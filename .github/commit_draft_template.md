# Edit the commit message below (lines starting with '#' are stripped by git).
# Commit with:  git commit -F "{{COMMIT_DRAFT_REL}}"
# Push with:    git push -u origin {{BRANCH}}
# Then tell the agent: "commit done"

{{COMMIT_TYPE}}({{SCOPE}}): {{PLAN_TITLE}}

{{BODY_BULLETS}}
